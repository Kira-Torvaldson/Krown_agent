# Scripts Krown Agent

Ce répertoire contient les scripts utilitaires du projet.

## 📄 Scripts

- **start-agent.sh** - Script de démarrage de l'agent

## 🚀 Utilisation

### Démarrage de l'agent

```bash
./scripts/start-agent.sh [socket_path]
```

Par défaut, utilise `/run/krown/krown-agent.sock`.

## 📝 Notes

Les scripts sont automatiquement copiés dans le conteneur Docker lors du build.

